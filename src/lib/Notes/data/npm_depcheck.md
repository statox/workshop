[tags]: # '["npm", "dependencies", "javascript", "package.json"]'
[title]: # 'NPM: Check unused dependencies'

The npm package [depcheck](https://www.npmjs.com/package/depcheck) is great for that:

```bash
npx depcheck
```
